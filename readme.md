全局固定规则，全程遵守
核心原则：在不影响质量的前提下，一定要最大程度的节约token。
开发只考虑全新安装模式。增加功能，效率优先，均不考虑兼容和升级。
数据查询禁止join联表。只能pk in 查询。
style.css 只添加共用样式。单页面使用样式，写到单页面里面。
仅做代码编辑、语法检测、纠错、格式优化、漏洞修复、新增功能；禁止模拟运行、执行代码、输出日志/调试内容/测试代码/调用示例。无多余解释、原理、总结、客套话、空行。不额外解读。不添加冗余话术。不生成多余注释、不拓展无关功能、不输出大段文本。